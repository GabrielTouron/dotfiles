return require("packer").startup(
	function(use)
		use("wbthomason/packer.nvim") -- Plugin package manager
		use("folke/tokyonight.nvim") -- Theme 
		use { "nvim-telescope/telescope.nvim", tag = '0.1.0', requires = { { 'nvim-lua/plenary.nvim' } } } -- Fuzzy finder
		use("nvim-lua/plenary.nvim") -- Lua utils
		use {
			"nvim-treesitter/nvim-treesitter",
			run = ':TSUpdate'
		} -- Language parser
		use("neovim/nvim-lspconfig") -- Make neovim smart
		use("williamboman/mason.nvim") -- LSP package manager
		use("williamboman/mason-lspconfig.nvim") -- LSP packages config
		use('hrsh7th/nvim-cmp') -- Autocompletion plugin
		use('hrsh7th/cmp-nvim-lsp') -- LSP source for nvim-cmp
		use('saadparwaiz1/cmp_luasnip') -- Snippets source for nvim-cmp
		use('L3MON4D3/LuaSnip') -- Snippets plugin
		use { "windwp/nvim-autopairs", config = function() require("nvim-autopairs").setup {{ "{}" }} end }
		use("mfussenegger/nvim-lint")

	end
)
